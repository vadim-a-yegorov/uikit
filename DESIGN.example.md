# DESIGN (BLANK)

This file intentionally contains no defaults. Every value below is a decision
this kit's other documents deliberately avoided making. Fill it once per
project.

## Color

- Primary palette:
- Semantic colors (success/warning/error/info):
- Background surface levels (base, raised, overlay):
- Text color tiers (primary, secondary, disabled):

## Typography

- Typeface(s):
- Type scale (sizes for: identifying field, operational field, contextual field, label, caption):
- Font weight rules:

## Spacing & Sizing

- Base spacing unit:
- Row height — Cozy tier:
- Row height — Compact tier:
- Row height — Condensed tier (if used):
- Icon sizes by context (toolbar, inline, dense table):

## Shape

- Border radius scale:
- Border widths/styles:
- Elevation/shadow scale:

## Motion

- Drag-start feedback (scale/opacity/shadow change):
- Drop-target valid/invalid visual treatment:
- Tooltip appear/dismiss delay and transition:
- Undo toast duration and transition:
- Row expand/collapse transition:

## Density

- Application-wide default density tier chosen:
- Justification (input device / task frequency / row count):
- User override mechanism and where it's stored:

## Detailness

- Entity name:
- Identifying fields:
- Operational fields:
- Contextual fields (on-demand tier):
- Diagnostic fields (on-demand tier):

## Icons

- Icon set/source:
- Icon meaning conventions specific to this product:

## Accessibility Tokens

- Minimum contrast ratios:
- Focus ring treatment:
- Reduced-motion fallback behavior:
